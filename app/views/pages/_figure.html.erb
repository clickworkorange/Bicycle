<% # TODO: handle keyboard focus without zooming (before pressing enter) %>
<figure>
    <button><%= thumbnail_image(image, version) %></button>
    <div class="zoom"><%= fullscreen_image(image) %></div>
    <figcaption><%= image.caption %></figcaption>
</figure>