
<style><%= banner_background_css(page) %></style>
<header style="">
  <div><%= content_tag(:h2, link_to(page.parent.title, full_page_path(page.parent))) if page.parent.present? %>
  <h1><%= page.title %></h1></div>
</header>