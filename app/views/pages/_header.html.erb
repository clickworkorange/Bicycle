<header style="background-image: url('<%= first_banner_or_blank(page, 1200) %>');">
  <div><%= content_tag(:h2, link_to(page.parent.title, page.parent)) if page.parent.present? %>
  <h1><%= page.title %></h1></div>
</header>