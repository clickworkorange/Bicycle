<h1>Pages</h1>

<div id="pages">
  <% @pages.each do |page| %>
    <%= render page %>
    <p>
      <%= link_to "Show this page", [:helm, page] %>
    </p>
  <% end %>
</div>

<%= link_to "New page", new_helm_page_path %>
