<p style="color: green"><%= notice %></p>

<h1>Images</h1>

<div id="images">
  <% @images.each do |image| %>
    <%= render image %>
    <p>
      <%= link_to "Show this image", [:helm, @page, image] %>
    </p>
  <% end %>
</div>

<%= link_to "New image", new_helm_page_image_path %>
